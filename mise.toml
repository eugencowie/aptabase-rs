[tools]
actionlint = "latest"
node = "latest"
"npm:@fission-ai/openspec" = "latest"
rust = "stable"

[tasks]
format = "cargo fmt"
lint = "cargo clippy --all-targets"
test = "cargo test"

[tasks.ci]
depends = ["format --check", "lint", "test"]
